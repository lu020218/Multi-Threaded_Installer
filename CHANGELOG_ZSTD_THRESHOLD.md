# ZSTD 分块压缩阈值调整

## 变更日期
2026-01-13

## 变更内容

### 大文件阈值调整
- **之前**: 10 MB
- **现在**: 128 MB

### 理由
10MB 的阈值过低，不适合作为"大文件"的标准。128MB 是更合理的阈值，能够：
1. 减少小文件的不必要开销
2. 更好地利用分块压缩的优势
3. 符合实际应用场景中的文件大小分布

### 影响的文件

#### 代码文件
- `src/packager/compression_module.cpp`
  - 第 163 行: `if (tarData.size() > 128 * 1024 * 1024)`
  - 第 184 行: 注释更新为 "小文件（≤ 128MB）"

#### 测试文件
- `test_zstd_blocks.cpp`
  - 测试数据大小从 20MB 增加到 160MB
  - 每个文件从 2MB 增加到 16MB

#### 文档文件
- `ZSTD_BLOCK_IMPLEMENTATION_SUMMARY.md`
  - 更新自动格式选择说明
  - 更新测试配置和结果
- `COMPRESSION_COMPARISON.md`
  - 更新算法选择示例代码

### 测试验证

#### 新测试结果（160MB 数据）
```
✓ 压缩速度: 125.2 MB/s
✓ 解压速度: 132.9 MB/s
✓ 块数量: 161 个块
✓ 数据完整性: 10/10 文件验证通过
✓ 格式检测: 正确识别块级格式
```

### 行为变化

#### 压缩行为
| 文件大小 | 之前 | 现在 |
|---------|------|------|
| < 10MB | 标准 ZSTD | 标准 ZSTD |
| 10MB - 128MB | 分块压缩 | 标准 ZSTD ⬅️ 变化 |
| > 128MB | 分块压缩 | 分块压缩 |

#### 优势
1. **减少开销**: 10-128MB 的文件不再使用分块格式，减少元数据开销
2. **更好的压缩比**: 标准 ZSTD 对中等文件的压缩比通常更好
3. **更快的压缩**: 避免了分块处理的额外开销
4. **保持并行解压**: 真正的大文件（>128MB）仍然受益于并行解压

### 向后兼容性
✓ 完全兼容 - 解压端自动检测格式，无需修改

### 建议
如果需要调整阈值，可以在 `compression_module.cpp` 中修改常量：
```cpp
const size_t BLOCK_COMPRESSION_THRESHOLD = 128 * 1024 * 1024; // 128MB
```

或者添加到配置文件中以支持运行时调整。
